/* eslint-disable import/prefer-default-export */

const defaults = {
  popoverBodyCloseButton: {
    selectors: '.popover__button',
    addActiveClass: false,
    activeClass: 'popover__button_activejs',
  },

  body: {
    selectors: '.popover__body',
    hiddenClass: 'popover_hidden',
  },
};

export { defaults };
